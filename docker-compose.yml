# Production docker-compose configuration
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dat-tools
    ports:
      - "8080:80"
    restart: unless-stopped
